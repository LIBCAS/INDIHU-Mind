export enum ColumnType {
  TEXT = "TEXT",
  BOOLEAN = "BOOLEAN",
  DATE = "DATE",
  DATETIME = "DATETIME",
  ENUM = "ENUM",
  LINK = "LINK",
}

export enum FormType {
  CREATE = "CREATE",
  EDIT = "EDIT",
}

export enum Order {
  ASC = "ASC",
  DESC = "DESC",
}

export const pageSizeOptions = [10, 20, 30, 40, 50];
